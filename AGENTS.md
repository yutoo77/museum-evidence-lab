# AGENTS.md

## プロジェクト方針

このリポジトリは、科学館職員向けの閉域型・根拠提示型ローカルRAGシステムです。

- UI文言は日本語とし、一般職員向けには専門用語を避ける。
- 外部APIへ文書本文や質問を送信しない。
- 実行時のモデル通信先は、設定で許可したローカルOllamaだけにする。
- 回答よりも、資料外回答の抑制と根拠の確認可能性を優先する。
- 思考過程を画面やログに保存しない。
- ログへ文書本文を必要以上に保存しない。
- `app.py`へ業務ロジックを集約しない。

## 開発環境

- Python 3.11（64bit）を使用する。
- 仮想環境は `.venv` に作成する。
- テストは `py -3.11 -m pytest` または仮想環境の `python -m pytest` で実行する。
- Ollama不要で検証できる処理は、必ずフェイクを使った単体テストにする。

## 完了確認

- `python -m pytest` が成功すること。
- `python -m compileall app.py src tests` が成功すること。
- UI変更時はStreamlit起動時の例外がないことを確認すること。
