# Museum Evidence Lab 利用ガイド

このアプリは、科学館職員向けローカルRAGの改善案を、既存版と比較するための実験用アプリです。RAGは「手元の資料を検索し、その資料を参照して回答する仕組み」です。回答の速さだけでなく、原文を確かめられること、資料だけでは答えられない質問を止められることを重視します。

標準画面に表示するのは、職員が根拠と内容を確認して使う「説明案」です。来館者へ自動で回答するための確定情報ではありません。画面の「利用前に根拠をご確認ください。」に従い、案内に使う前に引用と周辺文を確認してください。

比較元の既存版は別プロジェクトとして残しています。このリポジトリでは比較版のプログラムを配布し、登録した資料・索引・FAQ・職員確認済みQ&Aは各利用者のPC内に保存します。

## 1. 準備済みのPCで開く

フォルダ内の`start_lab.cmd`をダブルクリックしてください。必要な専用サーバーが起動し、[http://127.0.0.1:8502](http://127.0.0.1:8502)が開きます。ブラウザのタブを閉じただけではサーバーは停止しません。

初回の準備は[README](README.md#windowsで動かす)を参照してください。クローン先でPowerShellを開いて実行します。

```powershell
.\start_lab.cmd
```

仮想環境は他のPCへそのままコピーせず、移行先で作り直します。

初回クローンには登録済みの索引は含まれません。同梱の架空資料5件を次の手順で登録すると、実資料を用意せずに試せます。

画面が開いたら、次の順に操作します。サンプルを登録済みなら、手順2・3は不要です。

1. サイドバーの「AIを準備」を押す。検索用モデルと回答用モデルを事前に読み込みます。
2. 「資料を管理」を開き、「架空のサンプル資料を追加」を展開する。
3. 「サンプル5件を登録」を押す。登録資料の本文を確認できます。
4. 「資料を調べる」へ戻り、例えば「月の満ち欠けは、なぜ起こる？」と入力する。
5. 標準の「わかりやすく説明」、対象「一般向け」、長さ「標準」で「調べる」を押す。

説明文が表示されたら、各段落の「この説明の根拠」を開き、引用・資料名・ページを確認します。一つの段落に複数の引用がある場合は、すべて表示します。その中の「周辺の原文」を開くと、引用元の本文区画を資料名・ページとともに確認できます。表示する周辺文は検索で得た本文区画で、原本全体ではありません。同じ区画から複数の文を引用していても、周辺文は段落内に一度だけ表示します。「検索した原文」には検索された資料本文が入り、説明が完成する前から確認できます。引用・周辺文・検索結果は普段は折りたたまれ、説明本文へ混ぜません。「原文から回答」を選ぶと、従来どおり回答と原文を左右に並べます。

例えば「所要時間は35分です」という抜粋だけでは、どの体験の時間かがわかりません。周辺にある見出しや対象名、条件も確かめてください。周辺文を表示・点検していても、選ばれた引用だけで説明の全主張を支えられるとは限りません。抜粋に必要な条件が不足する場合もあり、根拠の意味の確認は残ります。

「処理の内訳」から、検索時間、回答処理の時間、モデルの読み込み時間などを確認できます。表示されない計測値をゼロと解釈しないでください。

初回の準備時間はPC・モデル・読み込み状態に左右されます。「AIを準備」は、必要な準備を利用前に行う機能であり、計算自体を省くものではありません。モデルがメモリから解放されると、再び読み込みが必要です。

停止するときは、このフォルダで実行します。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\stop_lab.ps1
```

比較版の記録済みプロセスと使用ポートを照合して停止します。既存版の8501や通常のOllamaの11434を停止するためのスクリプトではありません。

## 2. 既存版との使い分け

| | 既存版 | 比較版 |
|---|---|---|
| フォルダ | 従来のプロジェクトフォルダ | `museum-evidence-lab` |
| 起動ファイル | `start_app.cmd` | `start_lab.cmd` |
| ブラウザ | `127.0.0.1:8501` | `127.0.0.1:8502` |
| Ollama | `127.0.0.1:11434` | `127.0.0.1:11435` |
| 設定 | `config.yaml` | `lab_config.yaml` |
| 保存データ | 従来の`data/` | 比較版内の`data/lab/` |
| 評価の保存先 | `outputs/benchmarks/` | `outputs/lab-comparisons/` |

比較版では、専用Ollamaを`OLLAMA_NO_CLOUD=1`で起動します。起動スクリプトはクラウド無効化のログとポートの所有プロセスを確認し、確認できないサーバーをそのまま使わないようにしています。

モデルの重みファイル自体は、PCにインストール済みのOllamaの保存場所を共有します。分けているのは、アプリのコード、登録資料・検索索引・FAQ、実行プロセスとポートです。アプリで登録した資料がモデルの重みへ書き込まれる仕組みはありません。

両方の画面を開けても、同じPCのCPU・GPU・メモリを使います。速度を比較するときは他方で質問や資料登録を実行せず、条件を揃えてください。

## 3. 回答方法の違い

| 画面での名前 | 内部名 | 動作 | 確認する点 |
|---|---|---|---|
| わかりやすく説明（標準） | `explain` | 一つ以上の原文から説明案を生成し、同じモデルの点検を通した場合だけ表示。各段落に複数の引用を付けられる | 主語・数値の対象・理由・条件・否定を保ち、質問へ答え切れているか |
| 原文から回答 | `quoted` | AIが資料内の文を選び、プログラムがその原文をコピーして表示 | 文が実際に質問へ答えているか、原本の説明が正しいか |
| 短く言い換え（試験） | `concise` | 引用元の文を指定し、AIが短く言い換える | 意味の変更・省略・誤った補足がないか |
| 聞き返し・再検索（試験） | `bounded` | 原文選択に加え、語句による検索を最大1回追加する | 再検索が必要だったか、根拠や回答が改善したか |
| 従来の2段階生成（比較用） | `baseline` | 共通の索引で意味検索し、旧版の回答可否判定と生成を行う | 旧版の画面・保存・検索基盤まで同じ実行ではないこと |

普段の「回答方法」には「わかりやすく説明」「原文から回答」「比較用（試験）」を表示します。「比較用（試験）」を選ぶと、`concise`・`bounded`・`baseline`を選べます。既存方式を説明方式へ置き換えて削除したわけではありません。

`explain`では、対象を「一般向け」(`general`)・「子ども向け」(`child`)・「職員向け」(`staff`)、長さを「標準」(`standard`)・「短く」(`short`)から選べます。初期値は一般向け・標準です。子ども向けは小学生を想定した短い文とやさしい言葉、職員向けは理由・手順・注意条件を意識した指示をモデルへ渡します。指定どおりの読みやすさを保証する機能ではありません。ほかの回答方式では対象・長さの選択は無効になります。質問・方式・説明の対象や長さを変更すると、前の条件で出した回答を消します。

説明を生成する際は、検索された原文に識別番号を付け、モデルが段落ごとの説明文と根拠番号をJSONで返します。プログラムがその番号から原文・資料名・ページを対応付けるため、引用文そのものをモデルに作らせません。検索結果をそのまま回答扱いにする方式や、一文ずつの言い換えだけに制限する方式とは異なります。出典の存在・JSONの形式・重複・数値や単位の一部・途中終了を検査し、確認を通らない回答案は表示しません。停止理由だけを返した場合は、同時に生成された自由文を捨て、固定の保留・確認メッセージを表示します。停止理由と回答を混在させた出力も回答として採用しません。

`explain`の回答モデル呼び出しは最大2回です。1回目に説明案を生成して機械的な引用検査を通し、回答可能な案だけを2回目の点検へ進めます。2回目も同じローカルモデルを使い、質問・説明案・各段落に引用された原文を、その資料名と本文区画の文脈も含めて照合します。短い引用だけを見て、別の対象の数値や条件を転用しないための確認です。返させるのは最大32トークンの判定だけで、理由や思考過程は求めません。判定が`SUPPORTED`の場合だけ説明を表示し、ほかの判定・不正な応答・途中終了・制限時間超過では説明案を出しません。この点検は必須で、画面に追加の切り替え設定はありません。

検索で関連資料がなく停止する場合と、検索前の簡単な聞き返しでは0回です。1回目のモデル生成が根拠不足・矛盾・曖昧さで停止した場合には1回で終了します。2回目で保留になった場合は2回です。この回数は回答モデルの生成処理を指し、検索用の埋め込みや準備時の通信とは別です。

二段階にした理由は、開発用の実モデル確認で、引用文字列が正しくても質問とは違う対象を説明する例が出たためです。ただし同じモデルによる再確認は独立した正解判定ではなく、同じ誤りを見逃すことがあります。通過した回答にも`explanation_model_checked_not_guaranteed`という注意情報を残します。点検の失敗や慎重すぎる判定で、答えられる質問まで保留になる可能性と、待ち時間が増える可能性があります。実際の測定と判断は[EXPLANATION_RESULTS.md](EXPLANATION_RESULTS.md)で確認してください。

短い「これって何？」などの指示語質問には、検索前に展示名を尋ねます。`explain`では常にこの確認を通り、`quoted`・`concise`・`bounded`では承認済みFAQがない場合に行います。従来比較用の`baseline`には適用しません。説明生成時には、資料にない原因・設計意図・目的・安全性・効果を補わず、対象不明・情報不足・資料の矛盾があれば止めるよう指示しています。ただし、モデルによる判断が必ず正しいわけではありません。

**原文との一致を確認できても、説明の意味や科学的正確性、質問への適切さを保証したことにはなりません。** 例えば二つの数値が資料にあっても、それぞれを正しい展示へ対応付けているとは限りません。主語や因果関係の逆転、条件・否定の欠落、複数の問いへの回答漏れも内容確認が必要です。科学館の公式見解や、職員の確認を代替する機能ではありません。

`quoted`は従来の原文選択方式です。原文を選ぶモデル呼び出しは従来どおり最大1回で、説明方式の二度目の点検は追加していません。モデルとの受け渡しを`selection`配列のJSONとし、選択肢を実在する原文の番号か保留記号に限定します。プログラムが選ばれた原文をそのまま回答へコピーします。`concise`は一つの引用元の文を短く言い換える試験機能で、意味の整合性は自動検証できていません。「この説明の根拠」を開いて照合してください。

`bounded`は、小さく範囲を限定したエージェントの比較です。共通の事前確認と原文選択に加えて、検索資料から答えられないと判定された場合に、語句による検索を最大1回追加します。根拠が変わった場合だけ再度選択し、回答用モデルの呼び出しは最大2回です。矛盾が見つかった場合は再検索で上書きせず回答を控えます。外部検索や任意のプログラム実行は行いません。会話を記憶して展示物を特定する機能や、カメラ認識はまだありません。

矛盾への対処は、モデルへの指示に加え、**検索された原文の中で「同じ文言なのに、単位付きの数量だけが違う」場合の一部を保留する狭いルール**を設けています。言い換えられた文、条件や時点が違う文、一般的な科学的矛盾まで自動で判断する機能ではありません。原本同士の整合性は職員が確認します。

従来に近い`baseline`方式は、画面の「比較用（試験）」と比較評価コマンドで利用できます。共通のSQLite索引から意味検索の上位5件を取り、旧版の回答可否判定と生成処理を使います。旧アプリのChromaDB、画面、保存処理まで完全再現した測定ではありません。

## 4. 自分の資料を登録する

「資料を管理」でPDF・TXT・Markdownを選び、回答への使用を確認してから登録します。1ファイルの上限は50 MBです。画像だけのPDFは未対応で、文字を選択・コピーできるPDFが必要です。複雑な表、段組み、縦書きは抽出順が崩れることがあるため、登録後に「抽出された本文を確認」で確かめてください。

「適用日」は、その資料を回答に使い始める日です。未承認の資料と、まだ適用日になっていない資料は検索に使いません。同じ名前で再登録すると最新の内容へ置き換えます。

**同名資料を将来の日付で置き換えると、旧版はその時点で検索対象から外れ、新版も適用日までは利用されません。** 予約公開しながら旧版を自動維持する機能ではありません。すぐ使う資料の更新では、適用日を確認してください。旧原本の履歴を保存する文書管理システムでもないため、元ファイルは別途保管してください。

登録原本はSQLite内へ保存し、「登録した原本」から取り出せます。資料の内容や名前をファイルパスとして実行せず、画面でもプレーンテキストとして扱います。

検索では、文の意味を表す数値による検索と、日本語の連続する2文字・英数字を使った語句検索を組み合わせます。語句検索の点数は順位を決めるためのもので、正答確率ではありません。両方の順位を統合し、関連性の基準・重複・資料量を確認して、回答に渡す候補を選びます。

標準設定では、原文を最大800文字の区画へ分け、120文字を重ねます。回答用の資料本文は最大3区画・合計1,800文字です。途中を勝手に切って引用の意味を変えることを避けるため、区画全体を採用できない場合は、その区画を外します。この文字数は資料本文の制限であり、質問やモデルへの指示を含む総入力長とは異なります。

今回の説明方式追加では、この検索量・分割設定・距離しきい値`0.75`と、設定済みの回答モデル`qwen3:4b-instruct-2507-q4_K_M`を変更していません。文章の生成方式と画面の既定表示を変更したもので、検索範囲やモデル容量を増やした改善ではありません。

検索モデルの種類・重みの識別値・数値の次元・分割設定が既存の索引と一致しない場合は利用を止めます。検索モデルや分割方法を変更する際は、新しい保存先の索引を作り、原本を再登録する必要があります。

## 5. 職員確認済みQ&Aと従来のFAQ

標準の「わかりやすく説明」で回答と出典を確認したら、そのまま「この回答で問題ない」を選ぶか、「修正して保存」で文章を直して保存できます。保存されるのは元の質問・AIの説明案・職員が最終確認した文章・引用元の資料と版・日時です。職員が内容と周辺の原文を確認してから保存してください。保留や資料外質問は確認済みQ&Aとして保存できません。

似た質問では、同じ説明対象・長さの確認済みQ&Aを検索し、関連があるものだけ説明を作る際の参考候補へ加えます。確認済みの文章を無条件にそのまま返すものではありません。表示される引用は現在有効な館内資料から取り、通常の引用・数値の検査とローカルモデルによる確認を通します。画面の「参考にしました」は、候補が生成時の文脈へ渡されたことを示し、その文章が最終回答のどの表現に影響したかを証明するものではありません。

「確認済みQ&A」画面では、保存した質問・回答・根拠・状態を確認し、誤登録は個別に無効化できます。根拠資料が同名で更新されると「再確認が必要」になり、そのQ&Aは通常の回答へ使いません。更新後の内容を使う場合は、資料を確認して新しい回答を保存し直します。無効化後のQ&Aも再利用しません。確認済みでも永久に正しいとは限らず、AIの再学習でもありません。元資料自体が誤っている可能性や、資料と説明の意味のずれは、引き続き人が点検する必要があります。

従来の承認済みFAQは別の仕組みとして残ります。

標準の「わかりやすく説明」では、既存FAQを再利用せず、FAQ承認も表示しません。既存FAQは質問だけを照合し、説明の対象・長さ・生成方式を区別できないためです。子ども向けへ切り替えたのに一般向けの承認回答が返る、といった使い回しを避けています。複数の根拠をまとめた説明のFAQ保存にも対応していません。

「原文から回答」など従来方式のFAQ機能は残しています。回答が適切だと確認できた場合は、「この回答をFAQとして承認」を開きます。有効期限を指定し、「回答と引用原文を確認しました」をチェックして「承認して保存」を押します。`baseline`と承認済み回答自体には承認操作を表示しません。

以後、`quoted`・`concise`・`bounded`では、同じ質問に承認済みの回答を再利用します。毎回の文章生成が不要になるため、開館時間や定型的な案内など、人が確認できる回答に向いています。

- 質問は表記を正規化した後に完全一致で照合します。全角・半角、英字の大小、余分な空白は揃えますが、意味の近い別表現まで一致扱いにはしません。
- 指定した有効期限の日までは利用し、期限を過ぎると再利用しません。
- 根拠資料の内容・引用箇所を確認します。同名資料の更新などで引用先が現在の資料と一致しなくなると再利用しません。
- FAQには、承認した質問・回答・引用をローカル保存します。通常の利用ログとは別です。

承認操作は人による内容確認です。AIが自分の出力へ「正しい」と印を付ける機能ではありません。誤った資料を根拠に承認してしまえば、その誤りを再利用するため、期限と資料の更新管理が必要です。

「設定」→「承認済みFAQを整理」でFAQをまとめて削除できます。登録資料は保持します。

## 6. データと外部通信

通常のモデル通信は、このPCの許可済みモデルだけを対象にします。外部アドレス、クラウドモデル、HTTPの転送、環境変数に設定されたプロキシへの転送を拒否します。資料や質問に外部への送信を指示する文章があっても、そのためのツールはありません。検出できる不正な指示が資料に含まれる場合は回答を控えますが、あらゆる不正な指示を見分けられる保証はありません。

| 保存先 | 内容 | 扱い |
|---|---|---|
| `data/lab/index.sqlite3` | 登録原本、抽出本文、検索用数値、資料の状態、職員確認済みQ&A | 非公開の実行時データ |
| `data/lab/approved_faq.sqlite3` | 明示的に承認した質問・回答・引用・期限 | 非公開の実行時データ |
| `data/lab/audit/` | 有効化した場合の処理状態・所要時間など | 標準では無効。質問・回答・思考過程は記録しない |
| `data/lab/processes/` | 起動プロセスの記録とサーバーログ | ローカルの運用・不具合確認用 |
| `outputs/lab-comparisons/` | 比較用の索引、回答、引用、時間、手動評価表 | 通常の登録資料と分離した評価データ |

研究資料・学術資料・個人情報を使う場合は、利用許可と保存先を確認し、`data/lab/`を非公開で扱ってください。Gitの無視設定は、暗号化やアクセス制御ではありません。共有フォルダやクラウド同期先へフォルダ全体を置いたり、ZIPで渡したりすると、実行時データも共有される可能性があります。

実行時にネット接続を不要にするには、モデル・Python・依存パッケージを先に揃えます。必要なファイルを含めた移行と通信の確認は[オフライン導入手順](OFFLINE_DEPLOYMENT.md)を参照してください。

## 7. 新しいPCへの準備とモデル変更

初めて導入する場合は、Python 3.11（64bit）とOllamaを用意し、クローンした比較版フォルダで実行します。準備済みなら仮想環境を作り直す必要はありません。

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-lab.txt
```

このインストールは通常インターネットへ接続します。閉域PCへ直接接続できない場合は[オフライン導入手順](OFFLINE_DEPLOYMENT.md)に従って事前準備してください。

次に`start_lab.cmd`で専用Ollamaを起動します。必要なモデルがまだPCにない場合だけ、ネット接続がある準備段階で、別のPowerShellから取得します。

```powershell
$env:OLLAMA_HOST = '127.0.0.1:11435'
ollama pull embeddinggemma
ollama pull qwen3:4b-instruct-2507-q4_K_M
ollama list
```

`OLLAMA_HOST`はこのPowerShellの接続先指定です。既定のシステム設定を書き換える操作ではありません。モデル取得後、そのPowerShellは閉じて構いません。

画面の「設定」では、`lab_config.yaml`の許可リストにある回答モデルを選べます。未取得のモデルを選んでも自動ダウンロードはせず、準備に失敗します。変更後は「AIを準備」を押してください。

別モデルの取得も、準備段階で明示的に行います。例えば許可リスト内の候補を試す場合：

```powershell
$env:OLLAMA_HOST = '127.0.0.1:11435'
ollama pull qwen3.5:2b
```

大きい・新しいモデルが、このPCで速く正確に動くとは限りません。GPUメモリからあふれる場合や、指示への従い方が変わる場合があります。[比較結果](LAB_RESULTS.md)を確認し、同じ質問・資料で比較してください。回答モデルの切り替えでは検索索引を使い続けられますが、検索モデルを変更する場合は再登録が必要です。

## 8. 比較評価を実行する

比較用の資料は自作の架空データです。説明方式には[`benchmarks/explanation_dev/`](benchmarks/explanation_dev/README.md)の開発用ミニ評価を用意しています。複数資料の統合、対象・数値・単位の対応、条件・禁止の保持、情報不足・矛盾での停止などを、人が回答と全出典を読んで確認します。問題も要件も開発者に公開した開発用データであり、未知の問題に対する評価や専門家校閲、来館者による使いやすさ評価ではありません。

専用Ollamaが起動している状態で、比較版フォルダから実行します。一般向け・標準の説明を全ケースで確認する例です。モデルは現在の設定済みモデルを明示しています。

```powershell
.\.venv\Scripts\python.exe -m src.lab.evaluate --config lab_config.yaml --dataset benchmarks/explanation_dev/cases.jsonl --split dev --modes explain --models qwen3:4b-instruct-2507-q4_K_M --audience general --detail standard --run-id explanation-dev-general-01
```

子ども向け・短い説明は、別の実行IDで確認します。対象には`general`・`child`・`staff`、長さには`short`・`standard`を指定できます。これらの条件は`explain`にのみ適用し、結果・実行記録・人手確認表へ残します。

```powershell
.\.venv\Scripts\python.exe -m src.lab.evaluate --config lab_config.yaml --dataset benchmarks/explanation_dev/cases.jsonl --split dev --modes explain --models qwen3:4b-instruct-2507-q4_K_M --audience child --detail short --run-id explanation-dev-child-01
```

少数の動作確認には`--limit`を使えますが、先頭の質問だけでは停止すべき問題などが抜けます。その結果を全体の安全性やモデルの優劣の判断に使わないでください。

従来の[`benchmarks/lab_v2/`](benchmarks/lab_v2/README.md)には、原本を分けた開発用・留保用の問題があります。その内容や過去結果は説明用ミニ評価と混ぜずに残しています。従来データで方式を比較する例です。

```powershell
.\.venv\Scripts\python.exe -m src.lab.evaluate --config lab_config.yaml --dataset benchmarks/lab_v2/cases.jsonl --split dev --modes baseline quoted concise bounded explain --models qwen3:4b-instruct-2507-q4_K_M --run-id dev-comparison-01
```

取得済みの別モデルを比較する場合は、`--models`へ許可リスト内のモデル名を複数指定できます。モデルの自動取得や検索設定の変更は行いません。同じ質問を複数方式・モデルで繰り返す際は、実行順や計算の再利用の影響を区別してください。

同じ`--run-id`のフォルダは上書きしません。再実行時は名前を変えてください。比較が長い場合も、完了した試行は`progress.jsonl`に順次記録します。

結果は`outputs/lab-comparisons/<run-id>/<モデル>/`へ保存します。モデルごとに評価専用の索引を作り、アプリに登録した研究資料を混ぜません。

| `report/`内のファイル | 内容 |
|---|---|
| `report.json` | 回答・拒否・エラーの割合、速度などの自動集計 |
| `results.jsonl` | 各試行の回答、全引用と資料名・ページ・版、対象・長さ、処理時間、モデル計測値 |
| `manifest.json` | 原本・設定・コードの識別値、モデルの重み、実行条件 |
| `manual_review.csv` | 全出典を読み、意味・条件・対象・読みやすさ・拒否の妥当性を人が記入する表 |

質問・回答・引用を含む評価出力は、通常の利用ログより多くの内容を保存します。現在の同梱データは架空資料ですが、自分の評価データへ置き換えた場合は出力も非公開で管理してください。

初回と準備済みの時間を比べる際は、別の実行として準備条件を揃えます。`--first-query-state cold`や`warm`は実行者による状態の申告であり、モデルを停止・読み込みしたり、キャッシュを消したりする機能ではありません。同じ質問の再実行は計算を再利用できるため、初めての質問とは分けて読みます。

`holdout`は留保用です。開発用で方針を決めてから実行し、その結果を見て調整を繰り返した場合は、未使用の評価とは呼べません。「想定どおり回答した／控えた」「必要な語句が含まれた」「引用が存在する」のどれも、それ単独では科学的な正答率になりません。専門家による内容確認と、未使用の問題での追加評価が必要です。

全引用の原文一致は`exact_citation_matches`で確認できますが、それは説明の意味が根拠に支えられているという判定ではありません。人手確認表も自動で採点・記入されません。原文と回答が同じでないこと自体を、説明の正しさ・誤りのどちらの証拠にも扱わないでください。

現行の説明方式の確認結果は[EXPLANATION_RESULTS.md](EXPLANATION_RESULTS.md)、原文選択を中心とする過去の測定は[LAB_RESULTS.md](LAB_RESULTS.md)で区別します。過去の引用方式の速度や成功数を新方式の成績として読み替えず、各報告のコード・データ・モデル・準備条件を確認してください。速度や正答率の達成値を、このガイドで先に約束するものではありません。

## 9. コードを読む入口

```text
museum-evidence-lab/
├── lab_app.py                  # 職員向け画面
├── lab_config.yaml             # ローカルモデル、検索、保存、ログの設定
├── start_lab.cmd / .ps1        # 専用Ollamaと画面を起動
├── stop_lab.ps1                # 確認済みの比較版プロセスを停止
├── requirements-lab.txt        # 比較版の直接依存パッケージ
├── src/lab/
│   ├── contracts.py            # 根拠・複数引用付き回答・計測値の共通形式
│   ├── settings.py             # 設定の検証と保存先の制限
│   ├── client.py               # ローカル通信、モデル確認、時間計測
│   ├── index.py                # SQLite、資料更新、語句＋意味検索
│   ├── engine.py               # 説明・原文選択・限定再検索、期限と資料更新の確認
│   ├── explanation.py          # 説明と再点検の指示、複数出典JSON、数値などの検査
│   ├── grounding.py            # 原文の文への分割、共通の狭い出力検査
│   ├── faq.py                  # 承認済みFAQの照合と失効
│   ├── reviewed_qa.py          # 職員確認済みQ&Aのデータ形式
│   ├── audit.py                # 任意の運用メタデータ記録
│   ├── factory.py              # 設定から各部品を組み立てる
│   └── evaluate.py             # 比較評価CLIと出力
├── benchmarks/lab_v2/          # 架空原本、質問、評価条件
├── benchmarks/explanation_dev/ # 説明の開発用問題と人手確認要件
├── tests/test_lab_*.py         # フェイクモデル中心の回帰テスト
├── data/lab/                   # 非公開の実行時データ
└── outputs/lab-comparisons/    # 非公開の評価出力
```

既存の資料読み込み・分割処理を再利用し、比較版の検索・モデル通信・回答制御は`src/lab/`へ分けています。アプリの起動ファイルへ業務処理をまとめない構成です。旧版のファイルも比較元として残っています。

## 10. 開けない・答えないとき

| 状況 | 確認すること |
|---|---|
| ブラウザが接続できない | `start_lab.cmd`から起動したか。比較版のURLが8502か |
| 「AIの準備が必要」と表示される | 「AIを準備」を押したか。選択したモデルが取得済みか |
| 11435のサーバーを確認できない | `data/lab/processes/`の記録を確認する。別プロセスを無理に再利用しない |
| 資料が検索されない | 承認・適用日・抽出された本文を確認する。未登録や資料外質問では回答を控える |
| モデルや分割設定が一致しない | 元の設定へ戻すか、新しい索引へ原本を再登録する |
| 長く待つ・制限時間に達する | 大きいモデル、初回読込、他の推論処理、PCのメモリ使用量を確認する |
| 回答が要確認になる | 原文を確認する。途中終了や不正な引用を無理に回答扱いにしない |

比較版は単一PCでの検証を想定しています。認証・職員ごとの権限管理、複数人同時利用、実館での安全な運用は今後の評価対象です。
