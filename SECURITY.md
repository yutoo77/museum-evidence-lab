# セキュリティポリシー

## 対象とする運用

Museum Evidence Labは、信頼できる職員が単一PCで使うローカル比較プロトタイプです。GitHubで公開するのはソースコードと自作の架空資料であり、Web画面や登録済み資料を外へ公開するものではありません。

- Web UIは `127.0.0.1:8502`、専用Ollamaは `127.0.0.1:11435` に限定します。
- モデル名は設定の許可リストと実機のローカルモデル情報を照合します。
- HTTP転送、環境プロキシ、PC外アドレス、クラウドモデルは拒否します。
- 専用Ollamaのクラウド無効化と、起動・停止時のプロセス所有を確認します。
- 認証・職員別の権限管理・複数人同時利用は実装していません。

インターネットや館内LANへ直接公開しないでください。信頼できない人に文書を登録させる運用、職員が確認せず公式回答として使う運用は対象外です。同じPC上の他プロセスからのアクセスはローカルバインドだけでは防げません。

## 保存するデータ

| 保存先 | 内容 |
|---|---|
| `data/lab/index.sqlite3` | 登録した原本、抽出本文、埋め込み、資料の版・承認・適用日 |
| `data/lab/approved_faq.sqlite3` | 明示承認した質問、回答、引用、期限 |
| `data/lab/audit/` | 有効化した場合の状態・所要時間・呼出回数等。質問・回答・思考過程は記録しない |
| `data/lab/processes/` | 起動記録とサーバーログ |
| `outputs/` | 評価用の索引、質問・回答・引用、診断記録。実行によって内容は異なる |

通常の運用監査ログは標準で無効です。評価CLIの出力には質問・回答・引用が含まれるため、**評価結果だから公開してよいとは限りません**。公開用の記録には同梱の自作資料だけを使ってください。

保存内容を暗号化する機能はありません。`.gitignore`はアクセス制御でも暗号化でもありません。フォルダ全体のZIP化やクラウド同期では、Git管理外の資料も共有されます。原本の利用許可、端末のアクセス権、バックアップの扱いを別途確認してください。

旧`app.py`のログは別仕様で、`data/logs/interactions.jsonl`に質問・回答案を記録します。比較版の「本文を残さない運用監査」と混同しないでください。

## 回答と文書に対する防御の限界

ファイル種別・サイズ・保存名、資料版の変更、出典ID・形式・一部の数値、処理期限、生成の途中終了などを検査します。回答・ファイル名・原文はUIでプレーンテキストとして扱い、思考過程は保持しません。

ただし以下は未解決です。

- プロンプトインジェクションの完全な検出。
- 説明の全主張が原本から導けることの保証。確認モデルも対象・目的の取り違えを見逃します。
- 引用に含まれない前後の章・表・見出しまで含めた意味の把握。
- 不正なPDFに対する完全な隔離。信頼できないPDFを機密PCで解析しないでください。
- モデルの文脈長を正確に事前計算して入力切れを防ぐこと。
- OS・ブラウザ・Ollamaを含む端末全体の通信の遮断・監査。

[既知の失敗と評価範囲](EXPLANATION_RESULTS.md)、[閉域導入時の実機確認](OFFLINE_DEPLOYMENT.md)を参照してください。

## 依存関係の監査（2026-09-21）

`pip-audit`による`requirements-lab.txt`の解決済み45パッケージの確認では、既知の脆弱性は検出されませんでした。これはその時点の照合結果で、未知の脆弱性や将来の安全性を保証しません。全推移依存は固定していないため、新しい環境では結果が変わります。

旧版の再現用ChromaDB 1.5.9には次の4件の識別可能な報告があり、監査データに修正版の指定はありませんでした（監査出力5件のうち1件は重複）。

- [CVE-2026-45829：サーバー側コード注入](https://github.com/advisories/GHSA-f4j7-r4q5-qw2c)
- [CVE-2026-45833：サーバー側コード注入](https://github.com/advisories/GHSA-36p7-vc44-83pf)
- [CVE-2026-45831：権限の対象範囲の検証不足](https://github.com/advisories/GHSA-xph7-9rjv-w5fr)
- [CVE-2026-45830：テナント間の認可不足](https://github.com/advisories/GHSA-2wm9-hf6c-p5cr)

比較版はSQLiteを使用し、ChromaDBを通常の実行・開発依存に含めません。旧版を再現する場合だけ`requirements-legacy.txt`を明示的に使います。旧版もChroma HTTP Serverを公開しない構成ですが、そのことをパッケージ全体の安全保証とは扱いません。

## 公開・貢献時

`python tools/check_publication.py --include-untracked`は、公開候補に紛れた実行データ、モデル、代表的なトークン形式、個人PCのホームパスなどを検査します。疑わしい内容そのものは出力しません。この限定的な検査だけで全Secret・個人情報・利用権を確認できるわけではないため、差分を必ず目視で確認してください。

脆弱性の報告には公開Issueへ実データや秘密情報を貼らないでください。リポジトリの **Security → Report a vulnerability** を使い、報告機能が利用できない場合は公開Issueには機密を含まない連絡依頼だけを記載してください。
